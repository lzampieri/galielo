import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#a42c34'
        },
        secondary: {
            main: '#2c54bc'
        },
    },
});

export default theme;